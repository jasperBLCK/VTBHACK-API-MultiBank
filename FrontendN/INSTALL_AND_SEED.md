# Установка и заполнение базы данных

## Шаг 1: Установите зависимости

```bash
npm install
```

Это установит все необходимые пакеты, включая `dotenv` (если он еще не установлен).

## Шаг 2: Создайте файл .env.local

Создайте файл `.env.local` в корне проекта со следующим содержимым:

```
DATABASE_URL="file:./dev.db"
```

### Способы создания файла:

**Через PowerShell:**
```powershell
Set-Content -Path .env.local -Value 'DATABASE_URL="file:./dev.db"'
```

**Или скопируйте из шаблона:**
```powershell
Copy-Item env.template .env.local
```

**Или создайте вручную:**
1. Откройте Блокнот
2. Введите: `DATABASE_URL="file:./dev.db"`
3. Сохраните как `.env.local` в корне проекта

## Шаг 3: Инициализируйте базу данных

```bash
# Сгенерируйте Prisma Client
npx prisma generate

# Создайте базу данных и таблицы
npx prisma db push

# Заполните базу данных начальными данными
npx prisma db seed
```

## Готово! 🎉

Теперь можно запустить приложение:

```bash
npm run dev
```

## Если возникают проблемы

### Проблема: "Cannot find module 'dotenv'"
**Решение:** Запустите `npm install`

### Проблема: "Environment variable not found: DATABASE_URL"
**Решение:** 
1. Убедитесь, что файл `.env.local` существует
2. Проверьте содержимое: `Get-Content .env.local` в PowerShell
3. Или задайте переменную напрямую: `$env:DATABASE_URL="file:./dev.db"; npx prisma db seed`

### Проблема: База данных уже существует
**Решение:** Удалите файл `prisma/dev.db` и запустите команды заново

