# Устранение проблем - FrontendN интеграция

## Ошибка: "Не удалось подключиться к backend"

### Симптомы
- Ошибка `ECONNREFUSED` в консоли
- Сообщение "Backend недоступен" на странице входа
- Ошибка 503 при попытке авторизации

### Решение

1. **Проверьте что backend запущен:**
   ```bash
   python run.py
   ```
   
   Должно появиться:
   ```
   🏦 Starting My Awesome Bank on port 8001
   📍 Swagger UI: http://localhost:8001/docs
   ```

2. **Проверьте доступность backend:**
   ```bash
   # Используйте скрипт проверки
   node check-backend.js
   ```
   
   Или откройте в браузере: `http://localhost:8001/health`

3. **Проверьте конфигурацию:**
   
   Убедитесь что `FrontendN/.env.local` существует и содержит:
   ```bash
   NEXT_PUBLIC_BACKEND_URL=http://localhost:8001
   ```
   
   Если файла нет, создайте его:
   ```bash
   cd FrontendN
   cp env.template .env.local
   ```

4. **Проверьте порт:**
   
   Убедитесь что backend запущен на порту 8001 (или измените URL в `.env.local`)

5. **Перезапустите FrontendN:**
   
   После изменения `.env.local` перезапустите Next.js:
   ```bash
   # Остановите сервер (Ctrl+C)
   # Запустите снова
   npm run dev
   ```

## Ошибка: "Требуется авторизация"

### Симптомы
- Автоматическое перенаправление на `/login`
- Ошибка 401 при загрузке данных

### Решение

1. **Войдите в систему заново:**
   - Откройте `http://localhost:3000/login`
   - Введите учетные данные
   - Токен будет сохранен в `localStorage`

2. **Проверьте токен:**
   - Откройте DevTools (F12)
   - Перейдите в Application → Local Storage
   - Убедитесь что `access_token` существует

3. **Очистите localStorage:**
   ```javascript
   // В консоли браузера
   localStorage.clear()
   ```
   Затем войдите заново

## Ошибка: "Счета не найдены"

### Симптомы
- Пустой список счетов после входа
- Ошибка 404 при загрузке счетов

### Решение

1. **Проверьте что у клиента есть счета:**
   - Используйте тестового клиента: `team200-1`
   - Или создайте счета через API

2. **Проверьте авторизацию:**
   - Убедитесь что токен валиден
   - Проверьте заголовок `Authorization` в запросах

## Ошибка: "Ошибка выполнения перевода"

### Симптомы
- Ошибка при попытке перевести деньги
- Сообщение об ошибке валидации

### Решение

1. **Проверьте что оба счета существуют:**
   - Убедитесь что счет отправителя и получателя есть в системе

2. **Проверьте баланс:**
   - Убедитесь что на счете отправителя достаточно средств

3. **Проверьте формат данных:**
   - Сумма должна быть положительным числом
   - Описание должно быть строкой

## Общие проблемы

### FrontendN не запускается

1. **Проверьте зависимости:**
   ```bash
   cd FrontendN
   npm install
   ```

2. **Проверьте версию Node.js:**
   ```bash
   node --version
   ```
   Требуется Node.js 18+

3. **Очистите кеш:**
   ```bash
   rm -rf .next
   npm run dev
   ```

### Backend не запускается

1. **Проверьте зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Проверьте базу данных:**
   - Убедитесь что PostgreSQL запущен
   - Проверьте настройки в `env.txt`

3. **Проверьте порт:**
   - Убедитесь что порт 8001 свободен
   - Или измените порт в `run.py`

## Полезные команды

### Проверка подключения
```bash
# Проверка backend
node check-backend.js

# Проверка через curl
curl http://localhost:8001/health

# Проверка через браузер
open http://localhost:8001/health
```

### Просмотр логов
```bash
# Backend логи (в терминале где запущен backend)
# Логи отображаются автоматически

# FrontendN логи (в терминале где запущен Next.js)
# Логи отображаются автоматически
```

### Очистка кеша
```bash
# FrontendN
cd FrontendN
rm -rf .next
npm run dev

# Backend (перезапуск)
# Просто остановите и запустите снова: python run.py
```

## Контакты и поддержка

Если проблема не решена:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи backend в терминале
3. Проверьте логи FrontendN в терминале
4. Убедитесь что все зависимости установлены
5. Убедитесь что все сервисы запущены

