# 🚀 Быстрый старт

## Проблема: "Ошибка подключения к серверу"

Если вы видите ошибку `ECONNREFUSED` или "Backend недоступен", выполните следующие шаги:

### 1. Запустите Backend

Откройте **Терминал 1** и выполните:

```bash
python run.py
```

Дождитесь сообщения:
```
🏦 Starting My Awesome Bank on port 8001
📍 Swagger UI: http://localhost:8001/docs
```

### 2. Запустите FrontendN

Откройте **Терминал 2** и выполните:

```bash
cd FrontendN
npm install
npm run dev
```

### 3. Проверьте подключение

Откройте в браузере: `http://localhost:8001/health`

Должен вернуться JSON ответ со статусом "ok".

### 4. Настройте FrontendN

Убедитесь что файл `FrontendN/.env.local` существует и содержит:

```bash
NEXT_PUBLIC_BACKEND_URL=http://localhost:8001
```

Если файла нет, создайте его:
```bash
cd FrontendN
cp env.template .env.local
```

### 5. Откройте FrontendN

Откройте `http://localhost:3000` - на странице входа должно быть видно статус подключения к backend.

## ✅ Готово!

Теперь вы можете войти используя:
- Username: `team200-1`
- Password: `password`

## 📚 Дополнительная помощь

- **Подробное руководство:** `QUICK_START.md`
- **Устранение проблем:** `TROUBLESHOOTING.md`
- **Интеграция:** `INTEGRATION_GUIDE.md`

## 🔍 Проверка подключения

Используйте скрипт для проверки:
```bash
node check-backend.js
```

Или откройте в браузере: `http://localhost:8001/health`

