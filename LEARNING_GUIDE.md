# 📚 Что нужно знать для понимания кода Bank-in-a-Box

Руководство по необходимым знаниям и концепциям для работы с проектом.

---

## 🎯 Уровень подготовки

### Минимальный уровень:
- **Python 3.8+** - базовое знание синтаксиса
- **HTTP/REST API** - понимание запросов GET/POST, статусы ответов
- **JSON** - работа с JSON данными
- **Базы данных** - базовое понимание SQL

### Рекомендуемый уровень:
- **FastAPI** - современный Python веб-фреймворк
- **Асинхронное программирование** - async/await в Python
- **SQLAlchemy** - ORM для работы с БД
- **JWT (JSON Web Tokens)** - авторизация через токены
- **OpenBanking API** - стандарты открытого банкинга

---

## 🔧 Технологии и библиотеки

### 1. **Python 3.8+**
- **Что это:** Язык программирования
- **Что нужно знать:**
  - Базовый синтаксис (переменные, функции, классы)
  - Типы данных (str, int, dict, list)
  - Работа с модулями и импортами
  - Обработка исключений (try/except)
- **Где используется:** Весь проект написан на Python
- **Изучить:** [Python Tutorial](https://docs.python.org/3/tutorial/)

### 2. **FastAPI**
- **Что это:** Современный веб-фреймворк для создания API
- **Что нужно знать:**
  - Создание роутеров (`APIRouter`)
  - Обработка HTTP запросов (GET, POST, PUT, DELETE)
  - Параметры запросов (query params, path params, body)
  - Зависимости (Dependencies) - `Depends()`
  - Pydantic модели для валидации данных
- **Где используется:** Все файлы в `api/` - это FastAPI роутеры
- **Изучить:** [FastAPI Documentation](https://fastapi.tiangolo.com/)

**Пример из кода:**
```python
@router.post("/accounts")
async def create_account(
    request: CreateAccountRequest,
    current_client: dict = Depends(get_current_client),
    db: AsyncSession = Depends(get_db)
):
    # ...
```

### 3. **Асинхронное программирование (async/await)**
- **Что это:** Асинхронное выполнение кода для работы с I/O операциями
- **Что нужно знать:**
  - Ключевые слова `async` и `await`
  - Асинхронные функции и корутины
  - Асинхронные контекстные менеджеры (`async with`)
  - Асинхронные циклы событий (event loop)
- **Где используется:** Все функции в API используют `async/await` для работы с БД
- **Изучить:** [AsyncIO в Python](https://docs.python.org/3/library/asyncio.html)

**Пример из кода:**
```python
async def get_accounts(db: AsyncSession = Depends(get_db)):
    result = await db.execute(select(Account))
    accounts = result.scalars().all()
    return accounts
```

### 4. **SQLAlchemy (Async)**
- **Что это:** ORM (Object-Relational Mapping) для работы с базами данных
- **Что нужно знать:**
  - Модели (Models) - классы, представляющие таблицы БД
  - Сессии (Sessions) - для работы с БД
  - Запросы (Queries) - `select()`, `where()`, `join()`
  - Создание, чтение, обновление, удаление (CRUD)
  - Асинхронные операции - `await db.execute()`, `await db.commit()`
- **Где используется:** 
  - `models.py` - определение моделей
  - Все сервисы в `services/` - работа с БД
  - Все API в `api/` - получение данных из БД
- **Изучить:** [SQLAlchemy Async Documentation](https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html)

**Пример из кода:**
```python
# Модель
class Account(Base):
    __tablename__ = "accounts"
    id = Column(Integer, primary_key=True)
    account_number = Column(String, unique=True)
    balance = Column(Numeric)

# Запрос
result = await db.execute(
    select(Account).where(Account.status == "active")
)
accounts = result.scalars().all()
```

### 5. **PostgreSQL**
- **Что это:** Реляционная база данных
- **Что нужно знать:**
  - Базовые SQL запросы (SELECT, INSERT, UPDATE, DELETE)
  - Таблицы, колонки, типы данных
  - Связи между таблицами (Foreign Keys)
  - Транзакции (COMMIT, ROLLBACK)
- **Где используется:** Хранит все данные банка (клиенты, счета, платежи, согласия)
- **Изучить:** [PostgreSQL Tutorial](https://www.postgresql.org/docs/current/tutorial.html)

**Пример из кода:**
```sql
-- Структура таблицы accounts
CREATE TABLE accounts (
    id SERIAL PRIMARY KEY,
    account_number VARCHAR(20) UNIQUE,
    balance NUMERIC(15, 2),
    status VARCHAR(20),
    client_id INTEGER REFERENCES clients(id)
);
```

### 6. **JWT (JSON Web Tokens)**
- **Что это:** Способ авторизации через токены
- **Что нужно знать:**
  - Структура JWT (header.payload.signature)
  - Создание токенов (encoding)
  - Проверка токенов (decoding)
  - Алгоритмы подписи (HS256, RS256)
  - Полезная нагрузка (payload) - данные в токене
- **Где используется:** 
  - `services/auth_service.py` - создание и проверка токенов
  - Все API endpoints - проверка авторизации через `get_current_client()`
- **Изучить:** [JWT.io](https://jwt.io/)

**Пример из кода:**
```python
# Создание токена
token = create_access_token(
    data={"sub": "client-123", "type": "client"},
    use_rs256=False  # HS256 для клиентов
)

# Проверка токена
payload = verify_token(token)
client_id = payload["sub"]
```

### 7. **Pydantic**
- **Что это:** Библиотека для валидации данных
- **Что нужно знать:**
  - Модели данных (BaseModel)
  - Валидация полей (Field, validator)
  - Сериализация/десериализация
  - Автоматическая документация API
- **Где используется:** Все запросы и ответы API используют Pydantic модели
- **Изучить:** [Pydantic Documentation](https://docs.pydantic.dev/)

**Пример из кода:**
```python
class CreateAccountRequest(BaseModel):
    account_type: str
    currency: str = "RUB"
    name: Optional[str] = None
```

### 8. **HTTP и REST API**
- **Что это:** Протокол HTTP и архитектурный стиль REST
- **Что нужно знать:**
  - HTTP методы (GET, POST, PUT, DELETE)
  - HTTP статусы (200, 201, 400, 401, 404, 500)
  - Заголовки (Headers) - Authorization, Content-Type
  - Query parameters и path parameters
  - Request body и response body
  - CORS (Cross-Origin Resource Sharing)
- **Где используется:** Все API endpoints следуют REST принципам
- **Изучить:** [REST API Tutorial](https://restfulapi.net/)

**Пример из кода:**
```python
@router.get("/accounts/{account_id}")  # GET запрос с path parameter
async def get_account(account_id: str):
    # ...

@router.post("/payments")  # POST запрос с body
async def create_payment(request: PaymentRequest):
    # ...
```

### 9. **httpx**
- **Что это:** Асинхронная HTTP клиент-библиотека
- **Что нужно знать:**
  - Асинхронные HTTP запросы
  - GET, POST запросы
  - Заголовки и параметры
  - Обработка ответов
- **Где используется:** `api/multibank_proxy.py` - запросы к другим банкам
- **Изучить:** [httpx Documentation](https://www.python-httpx.org/)

**Пример из кода:**
```python
async with httpx.AsyncClient(timeout=30.0) as client:
    response = await client.post(
        f"{bank_url}/account-consents/request",
        json=consent_data,
        headers={"Authorization": f"Bearer {token}"}
    )
```

### 10. **OpenBanking Russia API**
- **Что это:** Стандарт открытого банкинга в России
- **Что нужно знать:**
  - Структура API (endpoints, методы)
  - Формат запросов и ответов
  - Согласия (Consents) - механизм авторизации доступа к данным
  - Payments API - стандарт для платежей
  - Accounts API - стандарт для счетов
- **Где используется:** 
  - `api/accounts.py` - Accounts API
  - `api/consents.py` - Account-Consents API
  - `api/payments.py` - Payments API
- **Изучить:** [OpenBanking Russia Wiki](https://wiki.opendatarussia.ru/)

---

## 🏗️ Архитектурные концепции

### 1. **Разделение на слои (Layered Architecture)**
- **API Layer** (`api/`) - обработка HTTP запросов, валидация
- **Service Layer** (`services/`) - бизнес-логика, работа с БД
- **Model Layer** (`models.py`) - определение структуры данных
- **Database Layer** (`database.py`) - подключение к БД

### 2. **Dependency Injection**
- **Что это:** Внедрение зависимостей через параметры функций
- **Где используется:** FastAPI `Depends()` для внедрения БД сессии, авторизации
- **Пример:**
```python
async def get_accounts(
    db: AsyncSession = Depends(get_db),  # Внедрение БД сессии
    current_client: dict = Depends(get_current_client)  # Внедрение клиента
):
    # ...
```

### 3. **Repository Pattern**
- **Что это:** Паттерн для работы с данными
- **Где используется:** Сервисы (`services/`) инкапсулируют логику работы с БД
- **Пример:** `ConsentService` содержит всю логику работы с согласиями

### 4. **Single Responsibility Principle**
- **Что это:** Каждый модуль отвечает за одну задачу
- **Где используется:** 
  - `api/auth.py` - только авторизация
  - `api/accounts.py` - только счета
  - `services/consent_service.py` - только согласия

---

## 🔐 Концепции безопасности

### 1. **JWT Авторизация**
- **Client Tokens (HS256)** - для клиентов банка
- **Bank Tokens (RS256)** - для межбанковских операций
- **Проверка токенов** - перед каждым запросом

### 2. **Согласия (Consents)**
- **Что это:** Механизм авторизации доступа к данным клиента
- **Как работает:**
  1. Банк запрашивает согласие
  2. Клиент одобряет/отклоняет
  3. Банк использует consent_id для доступа к данным
- **Где используется:** Все межбанковские запросы требуют согласия

### 3. **CORS (Cross-Origin Resource Sharing)**
- **Что это:** Механизм для разрешения запросов с других доменов
- **Где используется:** `main.py` - настройка CORS для федеративной платформы

---

## 📊 Структура данных

### 1. **Модели БД (models.py)**
- **Client** - клиенты банка
- **Account** - счета клиентов
- **Transaction** - транзакции
- **Payment** - платежи
- **Consent** - согласия
- **ConsentRequest** - запросы на согласия
- **Product** - продукты банка
- **ProductAgreement** - договоры с продуктами

### 2. **Формат данных OpenBanking**
- **Запросы:** JSON в формате OpenBanking Russia
- **Ответы:** JSON с полями `data`, `links`, `meta`
- **Пример:**
```json
{
  "data": {
    "account": [
      {
        "accountId": "acc-123",
        "status": "Enabled",
        "currency": "RUB",
        "account": [
          {
            "identification": "40817810099910004312"
          }
        ]
      }
    ]
  }
}
```

---

## 🎓 С чего начать изучение

### Шаг 1: Основы Python
1. Изучите синтаксис Python
2. Понимайте работу с классами и функциями
3. Изучите работу с модулями и пакетами

### Шаг 2: FastAPI
1. Пройдите [FastAPI Tutorial](https://fastapi.tiangolo.com/tutorial/)
2. Изучите работу с роутерами
3. Понимайте Pydantic модели
4. Изучите Dependencies

### Шаг 3: Асинхронность
1. Изучите `async/await` в Python
2. Понимайте асинхронные операции с БД
3. Изучите работу с `asyncio`

### Шаг 4: SQLAlchemy
1. Изучите основы ORM
2. Понимайте работу с моделями
3. Изучите асинхронные запросы
4. Понимайте связи между таблицами

### Шаг 5: JWT и безопасность
1. Изучите JWT токены
2. Понимайте HS256 и RS256 алгоритмы
3. Изучите механизм согласий (Consents)

### Шаг 6: OpenBanking API
1. Изучите стандарт OpenBanking Russia
2. Понимайте структуру API
3. Изучите Accounts API, Consents API, Payments API

### Шаг 7: Проект Bank-in-a-Box
1. Изучите структуру проекта
2. Начните с `api/auth.py` - простой модуль
3. Изучите `services/auth_service.py` - базовый сервис
4. Переходите к более сложным модулям

---

## 📖 Рекомендуемые ресурсы

### Документация:
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)
- [Python AsyncIO](https://docs.python.org/3/library/asyncio.html)
- [Pydantic Documentation](https://docs.pydantic.dev/)
- [JWT.io](https://jwt.io/)

### Курсы:
- [FastAPI Course](https://fastapi.tiangolo.com/tutorial/)
- [Python AsyncIO Course](https://realpython.com/async-io-python/)
- [SQLAlchemy Tutorial](https://docs.sqlalchemy.org/en/20/tutorial/)

### Книги:
- "FastAPI Modern Python Web Development" - Bill Lubanovic
- "Python Concurrency with asyncio" - Matthew Fowler
- "Architecture Patterns with Python" - Harry Percival

---

## 🔍 Как читать код проекта

### 1. Начните с простых модулей:
- `api/auth.py` - авторизация (простой модуль)
- `api/products.py` - продукты (только чтение)
- `services/auth_service.py` - базовый сервис

### 2. Изучите структуру:
- `models.py` - модели данных
- `config.py` - конфигурация
- `database.py` - подключение к БД

### 3. Переходите к сложным модулям:
- `api/accounts.py` - счета (с согласиями)
- `api/payments.py` - платежи (с межбанковскими переводами)
- `api/multibank_proxy.py` - агрегация счетов

### 4. Изучите сервисы:
- `services/consent_service.py` - управление согласиями
- `services/payment_service.py` - обработка платежей

---

## 💡 Советы для понимания кода

### 1. Читайте код сверху вниз:
- Начните с импортов - понимайте зависимости
- Изучите модели данных - понимайте структуру
- Читайте функции - понимайте логику

### 2. Используйте документацию:
- Читайте docstrings в коде
- Изучайте комментарии
- Смотрите примеры использования

### 3. Запускайте код:
- Запустите проект локально
- Тестируйте API через Swagger (`/docs`)
- Делайте запросы и смотрите ответы

### 4. Отлаживайте:
- Используйте логи (logging)
- Ставьте breakpoints
- Изучайте данные в БД

### 5. Задавайте вопросы:
- Изучайте ошибки
- Ищите решения в документации
- Читайте код других модулей

---

## 🎯 Чеклист знаний

Проверьте, что вы знаете:

- [ ] Python синтаксис и основы
- [ ] FastAPI - создание роутеров и endpoints
- [ ] async/await - асинхронное программирование
- [ ] SQLAlchemy - работа с БД через ORM
- [ ] PostgreSQL - базы данных
- [ ] JWT - авторизация через токены
- [ ] Pydantic - валидация данных
- [ ] HTTP/REST API - работа с API
- [ ] OpenBanking API - стандарты банкинга
- [ ] Dependency Injection - внедрение зависимостей
- [ ] Repository Pattern - работа с данными
- [ ] CORS - кросс-доменные запросы
- [ ] Согласия (Consents) - механизм авторизации

---

## 🚀 Быстрый старт

Если у вас уже есть базовые знания Python:

1. **Изучите FastAPI** (1-2 дня)
   - Пройдите tutorial
   - Создайте простой API

2. **Изучите async/await** (1 день)
   - Понимайте асинхронные функции
   - Изучите работу с asyncio

3. **Изучите SQLAlchemy** (1-2 дня)
   - Понимайте модели
   - Изучите запросы
   - Понимайте асинхронные операции

4. **Начните изучать проект** (2-3 дня)
   - Читайте код
   - Запускайте проект
   - Тестируйте API

5. **Изучите OpenBanking** (1-2 дня)
   - Понимайте стандарты
   - Изучите структуру API
   - Понимайте согласия

**Итого: 6-10 дней** для базового понимания проекта.

---

## ❓ Частые вопросы

### Q: Нужно ли знать Django/Flask для работы с FastAPI?
**A:** Нет, FastAPI проще и современнее. Но опыт с веб-фреймворками поможет.

### Q: Можно ли изучить async/await параллельно с проектом?
**A:** Да, но лучше изучить основы сначала, чтобы понимать код.

### Q: Нужно ли глубокое знание PostgreSQL?
**A:** Нет, достаточно базового SQL. SQLAlchemy упрощает работу с БД.

### Q: Обязательно ли знать OpenBanking для работы с проектом?
**A:** Нет, но понимание стандартов поможет понять логику API.

### Q: С какого модуля лучше начать?
**A:** Начните с `api/auth.py` - он простой и показывает базовые концепции.

---

## 📝 Заключение

Проект Bank-in-a-Box использует современные технологии и лучшие практики. Для понимания кода нужно знать:

1. **Python** - базовый уровень
2. **FastAPI** - веб-фреймворк
3. **async/await** - асинхронность
4. **SQLAlchemy** - ORM
5. **JWT** - авторизация
6. **OpenBanking API** - стандарты

Начните с основ и постепенно углубляйте знания. Используйте документацию и примеры кода. Запускайте проект и экспериментируйте!

Удачи в изучении! 🚀


