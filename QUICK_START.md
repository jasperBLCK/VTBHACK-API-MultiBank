# Быстрый старт - FrontendN интеграция

## Шаг 1: Настройка Backend

Убедитесь что `env.txt` содержит все необходимые настройки (уже настроено).

## Шаг 2: Настройка FrontendN

Создайте файл `FrontendN/.env.local`:

```bash
cd FrontendN
cp env.template .env.local
```

Или создайте вручную:
```bash
NEXT_PUBLIC_BACKEND_URL=http://localhost:8001
```

## Шаг 3: Запуск

### Терминал 1 - Backend:
```bash
python run.py
```

**Убедитесь что backend запустился и показывает:**
```
🏦 Starting My Awesome Bank on port 8001
📍 Swagger UI: http://localhost:8001/docs
```

### Терминал 2 - FrontendN:
```bash
cd FrontendN
npm install
npm run dev
```

**Проверка подключения:**
Если возникают ошибки подключения, проверьте что backend запущен:
```bash
# В корневой папке проекта
node check-backend.js
```

Или откройте в браузере: `http://localhost:8001/health`

## Шаг 4: Вход

1. Откройте `http://localhost:3000`
2. Войдите используя:
   - Username: `team200-1`
   - Password: `password`

## Готово! 🎉

Теперь вы можете использовать новый фронтенд FrontendN с существующими API эндпоинтами.

## Полезные ссылки

- Backend API: http://localhost:8001
- FrontendN: http://localhost:3000
- Swagger UI: http://localhost:8001/docs
- Старый фронтенд: http://localhost:8001/client/ (если нужно)

## Тестовые клиенты

- `team200-1` до `team200-10`
- `demo-client-001`, `demo-client-002`, `demo-client-003`
- Пароль: `password`

